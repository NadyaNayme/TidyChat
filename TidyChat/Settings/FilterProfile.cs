namespace TidyChat;

/// <summary>Built-in filter preset; applying one resets filter toggles to that template (customize freely afterward).</summary>
public enum FilterProfile
{
    Custom = 0,
    Balanced = 1,
    Minimal = 2,
    VanillaPlus = 3
}
